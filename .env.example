# Server Configuration
PORT=5000

# Recipient Email (where contact form submissions are sent)
RECIPIENT_EMAIL=your-email@gmail.com

# Mailgun SMTP Configuration (Primary)
MAILGUN_SMTP_USER=your-email@your-domain.mailgun.org
MAILGUN_SMTP_PASSWORD=your-smtp-password

# Mailgun API Configuration (Fallback)
MAILGUN_API_KEY=your-api-key-here
MAILGUN_DOMAIN=your-domain.mailgun.org